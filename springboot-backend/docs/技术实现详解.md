# 粤剧文化平台 — 技术实现详解

> 本文档用于毕业答辩，详细说明各核心模块的技术实现方案与代码位置。

---

## 目录

1. [项目整体架构](#1-项目整体架构)
2. [MySQL 数据库设计与建表（3NF）](#2-mysql-数据库设计与建表3nf)
3. [数据迁移、种子数据与演出样本](#3-数据迁移种子数据与演出样本)
4. [热度分析聚合接口与 Redis 缓存](#4-热度分析聚合接口与-redis-缓存)
5. [Spring Security + JWT + RBAC](#5-spring-security--jwt--rbac)
6. [BCrypt 密码加密](#6-bcrypt-密码加密)
7. [登录限流](#7-登录限流)
8. [XSS 防护](#8-xss-防护)
9. [操作日志（AOP）](#9-操作日志aop)
10. [管理后台数据接口与演出审核](#10-管理后台数据接口与演出审核)
11. [Redis 业务缓存](#11-redis-业务缓存)

---

## 1. 项目整体架构

### 1.1 技术栈

| 层级 | 技术选型 |
|------|---------|
| 后端框架 | Spring Boot 3.x |
| ORM 框架 | MyBatis-Plus |
| 安全框架 | Spring Security + JWT |
| 缓存中间件 | Redis |
| 数据库 | MySQL 8.0 |
| 构建工具 | Maven |
| 前端 | Vue 3 + Vant UI + ECharts |

### 1.2 项目分层结构

```
com.nghd.yueju
── web/          — Controller 层：REST 接口入口
├── service/      — Service 层：业务逻辑
├── mapper/       — Mapper 层：数据访问（MyBatis 注解）
├── entity/       — 实体类（MyBatis-Plus 注解）
├── security/     — 安全模块：JWT、认证过滤器、RBAC
├── common/       — 通用组件：异常处理、XSS 过滤、工具类
├── aop/          — AOP 切面：操作审计
├── config/       — 配置类：Redis、MyBatis-Plus、Swagger
└── dto/          — 数据传输对象
```

### 1.3 请求处理流程

```
客户端请求
    ↓
XssFilter（XSS 参数转义）
    ↓
JwtAuthFilter（JWT 解析 → SecurityContext）
    ↓
Spring Security（URL 权限校验）
    ↓
Controller → Service → Mapper → MySQL
    ↓
全局异常处理 → 统一 JSON 响应
```

**关键代码位置**：
- XSS 过滤器注册：[`XssFilter.java`](../springboot-backend/src/main/java/com/nghd/yueju/common/xss/XssFilter.java)
- JWT 认证过滤器：[`JwtAuthFilter.java`](../springboot-backend/src/main/java/com/nghd/yueju/security/JwtAuthFilter.java)
- 安全配置：[`SecurityConfig.java`](../springboot-backend/src/main/java/com/nghd/yueju/security/SecurityConfig.java)
- 全局异常处理：[`GlobalExceptionHandler.java`](../springboot-backend/src/main/java/com/nghd/yueju/common/exception/GlobalExceptionHandler.java)

---

## 2. MySQL 数据库设计与建表（3NF）

### 2.1 建表脚本

**文件位置**：[`sql/01-schema.sql`](../springboot-backend/sql/01-schema.sql)

### 2.2 表结构总览（15 张表）

#### 内容资料域（6 张表）

| 表名 | 主键 | 说明 | 3NF 设计要点 |
|------|------|------|-------------|
| `category` | `id` | 分类字典（行当/题材/年代/流派） | 独立字典表，消除名称重复存储 |
| `opera` | `id` | 剧目信息 | 流派/行当/年代存 category ID，不冗余名称 |
| `artist` | `id` | 演员信息 | 行当/流派存 category ID |
| `opera_artist` | `(opera_id, artist_id)` | 剧目-演员多对多关联 | 独立中间表，含角色字段 |
| `media` | `id` | 视听资源 | 通过 `opera_id`/`artist_id` 外键关联 |
| `lyrics` + `lyric_line` | `id` / `(lyrics_id, idx)` | 戏词及逐行 | 一对多拆分，避免 JSON 数组存储 |

#### 用户互动域（5 张表）

| 表名 | 主键 | 说明 | 3NF 设计要点 |
|------|------|------|-------------|
| `user` | `id` | 用户表 | 含 BCrypt 密码、角色字段，`username` 唯一 |
| `favorite` | `id` | 收藏 | `(user_id, type, target_id)` 联合唯一约束 |
| `comment` | `id` | 评论 | 冗余 `nickname` 避免频繁 JOIN |
| `learn_record` | `id` | 学唱进度 | `(user_id, lyrics_id)` 联合唯一 |
| `history` | `id` | 浏览历史 | 记录用户访问轨迹 |

#### 公开演出域（2 张表）

| 表名 | 主键 | 说明 | 3NF 设计要点 |
|------|------|------|-------------|
| `show_event` | `id` | 演出活动 | 申请人信息冗余存储，避免关联查询 |
| `show_signup` | `id` | 演出报名 | `(event_id, user_id)` 联合唯一，防重复报名 |

#### 热度数据域（2 张表）

| 表名 | 主键 | 说明 | 3NF 设计要点 |
|------|------|------|-------------|
| `performance` | `id` (自增) | 演出样本数据 | 数万条历史样本，含年份/地区/渠道/观众数 |
| `region_geo` | `code` | 地区地理信息 | 经纬度、是否核心流行区，`name` 唯一 |

#### 审计域（1 张表）

| 表名 | 主键 | 说明 |
|------|------|------|
| `audit_log` | `id` (自增) | 操作审计日志 |

### 2.3 索引设计

```sql
-- 剧目查询优化
KEY `idx_opera_genre` (`genre`)
KEY `idx_opera_troupe` (`troupe`)

-- 演出状态+日期联合索引（审核列表查询）
KEY `idx_evt_status` (`status`, `date`)

-- 评论按目标查询
KEY `idx_cmt_target` (`type`, `target_id`)

-- 热度分析
KEY `idx_perf_region` (`region`)
KEY `idx_perf_year` (`year`)
```

### 2.4 实体类映射

**文件位置**：[`entity/`](../springboot-backend/src/main/java/com/nghd/yueju/entity/)

使用 MyBatis-Plus 注解实现 ORM 映射：

```java
// User.java — 用户实体
@Data
@TableName("user")
public class User {
    @TableId(type = IdType.INPUT)
    private String id;
    private String username;
    private String password;      // BCrypt 加密存储
    private String nickname;
    private String role;          // "会员" / "管理员"
    // ...
}

// ShowEvent.java — 演出活动实体
@Data
@TableName("show_event")
public class ShowEvent {
    @TableId(type = IdType.INPUT)
    private String id;
    private String status;        // pending / approved / rejected
    private String applicantId;
    private String applicantName;
    // ...
}
```

---

## 3. 数据迁移、种子数据与演出样本

### 3.1 种子数据脚本

**文件位置**：[`sql/02-data.sql`](../springboot-backend/sql/02-data.sql)

### 3.2 数据内容

#### 分类数据（21 条）

```sql
-- 行当（6 个）：文武生、小生、正印花旦、老旦、丑生、武生
-- 题材（4 个）：历史、爱情、神话、公案、现代
-- 年代（3 个）：二三十年代、五六十年代、当代新编
-- 流派（6 个）：仙凤鸣、红派、马腔、薛腔、虾腔、芳腔
```

#### 剧目数据（12 部经典）

| 剧目 | 首演年份 | 编剧 | 人气值 |
|------|---------|------|--------|
| 帝女花 | 1957 | 唐涤生 | 9900 |
| 紫钗记 | 1957 | 唐涤生 | 8700 |
| 搜书院 | 1956 | 杨子静等 | 9200 |
| 关汉卿 | 1958 | 田汉 | 8000 |
| 胡不归 | 1939 | 冯志芬 | 8400 |
| ... | ... | ... | ... |

每部剧目包含：别名、摘要、高亮唱段、角色列表（JSON 数组）、名家列表（JSON 数组）。

#### 演员数据（12 位名家）

| 演员 | 行当 | 流派 | 人气值 |
|------|------|------|--------|
| 红线女 | 正印花旦 | 红派 | 9900 |
| 马师曾 | 丑生 | 马腔 | 9000 |
| 薛觉先 | 文武生 | 薛腔 | 8800 |
| 白雪仙 | 正印花旦 | 仙凤鸣 | 9100 |
| 任剑辉 | 文武生 | 仙凤鸣 | 9300 |
| ... | ... | ... | ... |

#### 关联数据（18 条）

`opera_artist` 表记录剧目与演员的对应关系及饰演角色：

```sql
('op_dnh', 'ar_rjf', '周世显'),   -- 帝女花 · 任剑辉 饰 周世显
('op_dnh', 'ar_bxx', '长平公主'), -- 帝女花 · 白雪仙 饰 长平公主
('op_ssy', 'ar_msz', '谢宝'),     -- 搜书院 · 马师曾 饰 谢宝
('op_ssy', 'ar_hxn', '翠莲'),     -- 搜书院 · 红线女 饰 翠莲
```

#### 演出样本数据

`performance` 表包含数万条历史演出记录，覆盖 2005-2024 年，字段包括：
- 地区、省份、场馆、演出团体
- 剧目 ID、剧目名称
- 演出日期、年份
- 现场观众数、线上播放量
- 渠道（剧场/电视/网络等）

---

## 4. 热度分析聚合接口与 Redis 缓存

### 4.1 核心文件

| 文件 | 路径 | 职责 |
|------|------|------|
| StatsMapper | [`StatsMapper.java`](../springboot-backend/src/main/java/com/nghd/yueju/mapper/StatsMapper.java) | SQL 聚合查询 |
| StatsService | [`StatsService.java`](../springboot-backend/src/main/java/com/nghd/yueju/service/StatsService.java) | 缓存封装层 |
| StatsController | [`StatsController.java`](../springboot-backend/src/main/java/com/nghd/yueju/web/StatsController.java) | REST 接口 |

### 4.2 复合热度指数算法

```
heat = SUM((现场观众 + 0.15 × 线上播放) × 年份权重)

年份权重 = 0.55 + 0.45 × (year - 最小年) / 年份跨度
```

**设计思想**：
- 现场观众权重为 1.0，线上播放权重为 0.15（线上观看门槛低）
- 年份权重从 0.55 线性递增至 1.0，越近的年份影响力越大
- 例如：数据跨度 20 年，2005 年权重 0.55，2024 年权重 1.0

**SQL 实现**（[`StatsMapper.java`](../springboot-backend/src/main/java/com/nghd/yueju/mapper/StatsMapper.java) 第 24-38 行）：

```java
@Select("""
    SELECT g.name AS name, g.lng AS lng, g.lat AS lat, g.is_core AS is_core,
           COUNT(p.id) AS shows,
           COALESCE(SUM(p.audience),0) AS audience,
           COALESCE(SUM(p.online_play),0) AS online,
           COALESCE(ROUND(SUM((p.audience + p.online_play*0.15) *
             (0.55 + 0.45*(p.year - #{a})/#{span}))),0) AS heat
    FROM region_geo g
    LEFT JOIN performance p ON p.region = g.name
      <if test='from != null'> AND p.year >= #{from} </if>
      <if test='to != null'>   AND p.year <= #{to} </if>
      <if test='channel != null'> AND p.channel = #{channel} </if>
    WHERE g.province = '广东'
    GROUP BY g.name, g.lng, g.lat, g.is_core
    ORDER BY heat DESC
    """)
List<Map<String, Object>> heatByCity(...);
```

### 4.3 聚合维度（7 个接口）

| 接口 | 方法 | 说明 |
|------|------|------|
| `GET /api/stats/heat` | `heat()` | 综合热度分析（城市/省份/热力点/年度趋势/渠道/热门剧目） |
| `GET /api/stats/overview` | `overview()` | 平台总览 KPI |
| `heatByCity` | 城市热度 | 广东省内城市分布，含经纬度用于地图渲染 |
| `heatByProvince` | 省份热度 | 全国省份排名 |
| `heatPoints` | 全球热力点 | 含海外地区（新加坡、吉隆坡等） |
| `perfByYear` | 年度趋势 | 按年份统计演出场次/观众/线上播放 |
| `perfByChannel` | 渠道分布 | 剧场/电视/网络等渠道对比 |
| `topOperas` | 热门剧目 TOP N | 按热度排序的剧目排行 |

### 4.4 Redis 缓存策略

**缓存封装**（[`StatsService.java`](../springboot-backend/src/main/java/com/nghd/yueju/service/StatsService.java) 第 31-44 行）：

```java
@SuppressWarnings("unchecked")
private <T> T cached(String key, Supplier<T> sup) {
    try {
        Object v = redis.opsForValue().get(key);
        if (v != null) return (T) v;   // 命中缓存
    } catch (Exception ignored) {}
    T r = sup.get();                    // 执行 SQL 聚合
    try {
        redis.opsForValue().set(key, r, TTL_SEC, TimeUnit.SECONDS);  // TTL 10 分钟
    } catch (Exception ignored) {}
    return r;
}
```

**缓存 Key 生成规则**（第 46-50 行）：

```java
private String key(String name, Object... parts) {
    StringBuilder sb = new StringBuilder("stats:").append(name);
    for (Object p : parts) sb.append(':').append(p);
    return sb.toString();
}
```

**示例**：
- `stats:city:2010:2024:剧场` — 2010-2024 年剧场渠道的城市热度
- `stats:top:2005:2024:null:10` — 全时段热门剧目 TOP 10
- `stats:overview:null:null:null` — 全时段总览

**缓存优势**：聚合涉及数万条 `performance` 记录，缓存后重复查询时延从秒级降至毫秒级。

---

## 5. Spring Security + JWT + RBAC

### 5.1 核心文件

| 文件 | 路径 | 职责 |
|------|------|------|
| SecurityConfig | [`SecurityConfig.java`](../springboot-backend/src/main/java/com/nghd/yueju/security/SecurityConfig.java) | 安全配置、URL 权限规则 |
| JwtUtil | [`JwtUtil.java`](../springboot-backend/src/main/java/com/nghd/yueju/security/JwtUtil.java) | JWT 签发与解析 |
| JwtAuthFilter | [`JwtAuthFilter.java`](../springboot-backend/src/main/java/com/nghd/yueju/security/JwtAuthFilter.java) | JWT 认证过滤器 |
| AuthUser | [`AuthUser.java`](../springboot-backend/src/main/java/com/nghd/yueju/security/AuthUser.java) | 用户上下文 Record |
| CurrentUser | [`CurrentUser.java`](../springboot-backend/src/main/java/com/nghd/yueju/security/CurrentUser.java) | 线程安全获取当前用户 |

### 5.2 无状态认证架构

**配置**（[`SecurityConfig.java`](../springboot-backend/src/main/java/com/nghd/yueju/security/SecurityConfig.java) 第 39-41 行）：

```java
http
    .csrf(AbstractHttpConfigurer::disable)   // 禁用 CSRF（JWT 无状态）
    .cors(c -> {})                            // 启用 CORS
    .sessionManagement(s -> s.sessionCreationPolicy(SessionCreationPolicy.STATELESS))  // 无 Session
```

### 5.3 JWT 签发与解析

**签发**（[`JwtUtil.java`](../springboot-backend/src/main/java/com/nghd/yueju/security/JwtUtil.java) 第 32-43 行）：

```java
public String generate(AuthUser u) {
    Date now = new Date();
    return Jwts.builder()
            .subject(u.id())                          // JWT sub = 用户 ID
            .claim("username", u.username())          // 用户名
            .claim("nickname", u.nickname())          // 昵称
            .claim("role", u.role())                  // 角色
            .claim("avatarSeed", u.avatarSeed())      // 头像种子
            .issuedAt(now)
            .expiration(new Date(now.getTime() + expireMs))  // 默认 7 天
            .signWith(key)                            // HS256 签名
            .compact();
}
```

**解析**（第 45-51 行）：

```java
public AuthUser parse(String token) {
    Claims c = Jwts.parser().verifyWith(key).build()
            .parseSignedClaims(token).getPayload();
    return new AuthUser(c.getSubject(), c.get("username", String.class),
            c.get("nickname", String.class), c.get("role", String.class),
            c.get("avatarSeed", Integer.class));
}
```

**密钥配置**（[`application.yml`](../springboot-backend/src/main/resources/application.yml) 第 31-33 行）：

```yaml
app:
  jwt:
    secret: ${JWT_SECRET:nanguo-hongdou-yueju-jwt-secret-key-...}
    expire-minutes: ${JWT_EXPIRE_MINUTES:10080}   # 7 天
```

### 5.4 JWT 认证过滤器

**核心逻辑**（[`JwtAuthFilter.java`](../springboot-backend/src/main/java/com/nghd/yueju/security/JwtAuthFilter.java) 第 33-50 行）：

```java
@Override
protected void doFilterInternal(HttpServletRequest req, HttpServletResponse resp, FilterChain chain)
        throws ServletException, IOException {
    String token = resolveToken(req);  // 从 Authorization 或 x-token 头获取
    if (token != null && SecurityContextHolder.getContext().getAuthentication() == null) {
        try {
            // 1. 检查 Redis 注销黑名单
            boolean revoked = Boolean.TRUE.equals(redis.hasKey(BLACKLIST_PREFIX + token));
            if (!revoked) {
                // 2. 解析 JWT 获取用户信息
                AuthUser u = jwtUtil.parse(token);
                // 3. 根据角色映射权限
                String authority = u.isAdmin() ? "ROLE_ADMIN" : "ROLE_MEMBER";
                var auth = new UsernamePasswordAuthenticationToken(
                        u, token, List.of(new SimpleGrantedAuthority(authority)));
                // 4. 设置到 SecurityContext
                SecurityContextHolder.getContext().setAuthentication(auth);
            }
        } catch (Exception ignored) {
            // 令牌无效/过期：保持未认证
        }
    }
    chain.doFilter(req, resp);
}
```

**Token 提取**（第 52-56 行）：

```java
private String resolveToken(HttpServletRequest req) {
    String h = req.getHeader("Authorization");
    if (h != null && h.startsWith("Bearer ")) return h.substring(7);
    return req.getHeader("x-token");  // 兼容旧版前端
}
```

### 5.5 RBAC 权限控制

**URL 权限规则**（[`SecurityConfig.java`](../springboot-backend/src/main/java/com/nghd/yueju/security/SecurityConfig.java) 第 42-52 行）：

```java
.authorizeHttpRequests(reg -> reg
    // 静态资源公开
    .requestMatchers("/", "/index.html", "/admin", "/admin.html",
            "/css/**", "/js/**", "/assets/**", "/vendor/**", "/favicon.ico",
            "/swagger-ui/**", "/swagger-ui.html", "/v3/api-docs/**").permitAll()
    // 管理后台：需要管理员角色
    .requestMatchers("/api/admin/**").hasRole("ADMIN")
    // 注册/登录公开
    .requestMatchers(HttpMethod.POST, "/api/auth/login", "/api/auth/register").permitAll()
    // AI 助手公开
    .requestMatchers("/api/assistant/**").permitAll()
    // 内容查询公开（游客可浏览）
    .requestMatchers(HttpMethod.GET,
            "/api/categories", "/api/operas/**", "/api/artists/**",
            "/api/media/**", "/api/lyrics/**", "/api/shows", "/api/shows/*",
            "/api/comments", "/api/stats/**", "/api/health").permitAll()
    // 其他 API 需要登录
    .requestMatchers("/api/**").authenticated()
    .anyRequest().permitAll())
```

**角色映射**（[`AuthUser.java`](../springboot-backend/src/main/java/com/nghd/yueju/security/AuthUser.java) 第 7-9 行）：

```java
public record AuthUser(String id, String username, String nickname, String role, Integer avatarSeed) {
    public boolean isAdmin() {
        return "管理员".equals(role);
    }
}
```

### 5.6 异常处理

**认证/授权异常**（[`SecurityConfig.java`](../springboot-backend/src/main/java/com/nghd/yueju/security/SecurityConfig.java) 第 53-55 行）：

```java
.exceptionHandling(e -> e
    .authenticationEntryPoint((req, resp, ex) -> writeError(resp, 401, "请先登录"))
    .accessDeniedHandler((req, resp, ex) -> writeError(resp, 403, "需要管理员权限")))
```

**全局异常处理**（[`GlobalExceptionHandler.java`](../springboot-backend/src/main/java/com/nghd/yueju/common/exception/GlobalExceptionHandler.java)）：

```java
@RestControllerAdvice
public class GlobalExceptionHandler {
    @ExceptionHandler(BusinessException.class)
    public ResponseEntity<Map<String, Object>> handleBusiness(BusinessException e) {
        return ResponseEntity.status(e.getStatus()).body(Map.of("error", e.getMessage()));
    }

    @ExceptionHandler(MethodArgumentNotValidException.class)
    public ResponseEntity<Map<String, Object>> handleValid(MethodArgumentNotValidException e) {
        FieldError fe = e.getBindingResult().getFieldError();
        String msg = fe != null ? fe.getDefaultMessage() : "参数校验失败";
        return ResponseEntity.status(400).body(Map.of("error", msg));
    }

    @ExceptionHandler(AccessDeniedException.class)
    public ResponseEntity<Map<String, Object>> handleDenied(AccessDeniedException e) {
        return ResponseEntity.status(403).body(Map.of("error", "需要管理员权限"));
    }

    @ExceptionHandler(AuthenticationException.class)
    public ResponseEntity<Map<String, Object>> handleAuth(AuthenticationException e) {
        return ResponseEntity.status(401).body(Map.of("error", "请先登录"));
    }
}
```

### 5.7 注销机制（JWT 黑名单）

**注销逻辑**（[`AuthService.java`](../springboot-backend/src/main/java/com/nghd/yueju/service/AuthService.java) 第 93-97 行）：

```java
public void logout(String token) {
    if (token != null && !token.isBlank()) {
        // 将 token 写入 Redis 黑名单，TTL = JWT 剩余有效期
        redis.opsForValue().set(JwtAuthFilter.BLACKLIST_PREFIX + token, "1",
                jwtUtil.getExpireMs(), TimeUnit.MILLISECONDS);
    }
}
```

**过滤器拦截**（[`JwtAuthFilter.java`](../springboot-backend/src/main/java/com/nghd/yueju/security/JwtAuthFilter.java) 第 38-39 行）：

```java
boolean revoked = Boolean.TRUE.equals(redis.hasKey(BLACKLIST_PREFIX + token));
if (!revoked) { /* 正常认证 */ }
```

---

## 6. BCrypt 密码加密

### 6.1 核心文件

**PasswordEncoder Bean**（[`SecurityConfig.java`](../springboot-backend/src/main/java/com/nghd/yueju/security/SecurityConfig.java) 第 31-33 行）：

```java
@Bean
public PasswordEncoder passwordEncoder() {
    return new BCryptPasswordEncoder();
}
```

### 6.2 注册加密

**文件**：[`AuthService.java`](../springboot-backend/src/main/java/com/nghd/yueju/service/AuthService.java) 第 52-67 行

```java
public Map<String, Object> register(AuthDtos.Register dto) {
    if (findByUsername(dto.username()) != null) {
        throw BusinessException.conflict("该账号已被注册");
    }
    User u = new User();
    u.setId(Ids.uid("u"));
    u.setUsername(dto.username());
    u.setPassword(encoder.encode(dto.password()));  // BCrypt 加密
    u.setNickname(dto.nickname() != null && !dto.nickname().isBlank() ? dto.nickname() : dto.username());
    u.setRole("会员");
    u.setAvatarSeed((int) (Math.random() * 6));
    u.setCreated(Ids.now());
    userMapper.insert(u);
    return tokenResult(u);
}
```

### 6.3 登录校验

**文件**：[`AuthService.java`](../springboot-backend/src/main/java/com/nghd/yueju/service/AuthService.java) 第 69-82 行

```java
public Map<String, Object> login(AuthDtos.Login dto) {
    // ... 限流检查 ...
    User u = findByUsername(dto.username());
    if (u == null || !encoder.matches(dto.password(), u.getPassword())) {
        // 密码不匹配
        throw BusinessException.unauthorized("账号或密码错误");
    }
    // 登录成功
    return tokenResult(u);
}
```

### 6.4 改密

**文件**：[`AuthService.java`](../springboot-backend/src/main/java/com/nghd/yueju/service/AuthService.java) 第 118-127 行

```java
public void changePassword(AuthDtos.ChangePwd dto, String currentToken) {
    AuthUser au = CurrentUser.require();
    User u = userMapper.selectById(au.id());
    if (u == null || !encoder.matches(dto.oldPwd(), u.getPassword())) {
        throw BusinessException.badRequest("原密码不正确");
    }
    u.setPassword(encoder.encode(dto.newPwd()));  // 新密码 BCrypt 加密
    userMapper.updateById(u);
}
```

---

## 7. 登录限流

### 7.1 核心文件

**文件**：[`AuthService.java`](../springboot-backend/src/main/java/com/nghd/yueju/service/AuthService.java)

### 7.2 限流参数

```java
private static final int MAX_FAIL = 5;      // 最多失败 5 次
private static final long WINDOW_SEC = 600; // 时间窗口 10 分钟
```

### 7.3 限流实现

**登录前检查**（第 70-74 行）：

```java
String key = "login:fail:" + dto.username().toLowerCase();
Object cnt = redis.opsForValue().get(key);
if (cnt != null && Integer.parseInt(cnt.toString()) >= MAX_FAIL) {
    throw BusinessException.of(429, "尝试过于频繁，请稍后再试");
}
```

**失败计数**（第 78-80 行）：

```java
Long c = redis.opsForValue().increment(key);
if (c != null && c == 1L) redis.expire(key, WINDOW_SEC, TimeUnit.SECONDS);
throw BusinessException.unauthorized("账号或密码错误");
```

**成功清除**（第 81 行）：

```java
redis.delete(key);
```

### 7.4 限流流程

```
用户登录
    ↓
检查 Redis: login:fail:{username}
    ↓
计数 >= 5？ → 是 → 返回 429 "尝试过于频繁"
    ↓ 否
查询用户 → 密码校验
    ↓ 失败
Redis increment → 首次设置 600s 过期 → 返回 401
    ↓ 成功
Redis delete → 签发 JWT → 返回 200
```

---

## 8. XSS 防护

### 8.1 核心文件

| 文件 | 路径 | 职责 |
|------|------|------|
| XssFilter | [`XssFilter.java`](../springboot-backend/src/main/java/com/nghd/yueju/common/xss/XssFilter.java) | 过滤器注册 |
| XssRequestWrapper | [`XssRequestWrapper.java`](../springboot-backend/src/main/java/com/nghd/yueju/common/xss/XssRequestWrapper.java) | 参数转义 |

### 8.2 过滤器注册

**文件**：[`XssFilter.java`](../springboot-backend/src/main/java/com/nghd/yueju/common/xss/XssFilter.java)

```java
@Configuration
public class XssFilter {
    @Bean
    public FilterRegistrationBean<Filter> xssFilterRegistration() {
        FilterRegistrationBean<Filter> bean = new FilterRegistrationBean<>();
        bean.setFilter(new Filter() {
            @Override
            public void doFilter(ServletRequest req, ServletResponse resp, FilterChain chain)
                    throws IOException, ServletException {
                chain.doFilter(new XssRequestWrapper((HttpServletRequest) req), resp);
            }
        });
        bean.addUrlPatterns("/api/*");  // 拦截所有 API 请求
        bean.setOrder(1);               // 优先级最高
        return bean;
    }
}
```

### 8.3 参数转义

**文件**：[`XssRequestWrapper.java`](../springboot-backend/src/main/java/com/nghd/yueju/common/xss/XssRequestWrapper.java)

```java
public class XssRequestWrapper extends HttpServletRequestWrapper {
    public XssRequestWrapper(HttpServletRequest request) {
        super(request);
    }

    @Override
    public String getParameter(String name) {
        return clean(super.getParameter(name));
    }

    @Override
    public String[] getParameterValues(String name) {
        String[] vs = super.getParameterValues(name);
        if (vs == null) return null;
        String[] out = new String[vs.length];
        for (int i = 0; i < vs.length; i++) out[i] = clean(vs[i]);
        return out;
    }

    private String clean(String v) {
        return v == null ? null : HtmlUtils.htmlEscape(v);  // Spring HtmlUtils 转义
    }
}
```

### 8.4 转义效果

| 原始输入 | 转义后 |
|---------|--------|
| `<script>alert(1)</script>` | `&lt;script&gt;alert(1)&lt;/script&gt;` |
| `"><img src=x onerror=alert(1)>` | `&quot;&gt;&lt;img src=x onerror=alert(1)&gt;` |
| `javascript:alert(1)` | `javascript:alert(1)`（仅 HTML 实体转义） |

**防护范围**：反射型 XSS（URL 参数、表单参数）。存储型 XSS（如评论内容）在 Service 层另行处理。

---

## 9. 操作日志（AOP）

### 9.1 核心文件

| 文件 | 路径 | 职责 |
|------|------|------|
| AuditAspect | [`AuditAspect.java`](../springboot-backend/src/main/java/com/nghd/yueju/aop/AuditAspect.java) | AOP 切面 |
| AuditLog 注解 | [`aop/AuditLog.java`](../springboot-backend/src/main/java/com/nghd/yueju/aop/AuditLog.java) | 自定义注解 |
| AuditLog 实体 | [`entity/AuditLog.java`](../springboot-backend/src/main/java/com/nghd/yueju/entity/AuditLog.java) | 日志实体 |
| AuditLogMapper | [`AuditLogMapper.java`](../springboot-backend/src/main/java/com/nghd/yueju/mapper/AuditLogMapper.java) | 数据访问 |

### 9.2 自定义注解

**文件**：[`aop/AuditLog.java`](../springboot-backend/src/main/java/com/nghd/yueju/aop/AuditLog.java)

```java
@Target(ElementType.METHOD)
@Retention(RetentionPolicy.RUNTIME)
public @interface AuditLog {
    String value();   // 操作类型，如 "login" / "register" / "review_event"
}
```

### 9.3 AOP 切面实现

**文件**：[`AuditAspect.java`](../springboot-backend/src/main/java/com/nghd/yueju/aop/AuditAspect.java)

```java
@Aspect
@Component
public class AuditAspect {

    private final AuditLogMapper auditLogMapper;

    @Around("@annotation(auditLog)")
    public Object around(ProceedingJoinPoint pjp, com.nghd.yueju.aop.AuditLog auditLog) throws Throwable {
        Object result = pjp.proceed();  // 执行目标方法
        try {
            AuthUser u = CurrentUser.get();
            AuditLog log = new AuditLog();
            log.setTime(Ids.now());
            log.setUserId(u != null ? u.id() : null);
            log.setUsername(u != null ? u.username() : null);
            log.setAction(auditLog.value());
            log.setIp(clientIp());      // 获取客户端 IP
            auditLogMapper.insert(log);
        } catch (Exception ignored) {
            // 审计失败不影响主流程
        }
        return result;
    }

    private String clientIp() {
        ServletRequestAttributes attr = (ServletRequestAttributes)
                RequestContextHolder.currentRequestAttributes();
        HttpServletRequest req = attr.getRequest();
        String xf = req.getHeader("x-forwarded-for");
        if (xf != null && !xf.isBlank()) return xf.split(",")[0].trim();
        return req.getRemoteAddr();
    }
}
```

### 9.4 使用方式

在需要审计的方法上添加 `@AuditLog` 注解：

```java
@AuditLog("login")
public Map<String, Object> login(AuthDtos.Login dto) { ... }

@AuditLog("register")
public Map<String, Object> register(AuthDtos.Register dto) { ... }

@AuditLog("review_event")
public Map<String, Object> review(String id, String action, String note, AuthUser admin) { ... }
```

### 9.5 日志表结构

**文件**：[`sql/01-schema.sql`](../springboot-backend/sql/01-schema.sql) 第 241-251 行

```sql
CREATE TABLE `audit_log` (
  `id` BIGINT NOT NULL AUTO_INCREMENT,
  `time` VARCHAR(32),       -- 操作时间
  `user_id` VARCHAR(64),    -- 用户 ID
  `username` VARCHAR(64),   -- 用户名
  `action` VARCHAR(64),     -- 操作类型
  `detail` VARCHAR(255),    -- 操作详情
  `ip` VARCHAR(64),         -- 客户端 IP
  PRIMARY KEY (`id`),
  KEY `idx_audit_user` (`user_id`)
);
```

### 9.6 审计日志记录的操作类型

| 操作类型 | 触发场景 |
|---------|---------|
| `register` | 用户注册 |
| `login` | 用户登录 |
| `change_password` | 修改密码 |
| `review_event` | 管理员审核演出 |

---

## 10. 管理后台数据接口与演出审核

### 10.1 核心文件

| 文件 | 路径 | 职责 |
|------|------|------|
| AdminController | [`AdminController.java`](../springboot-backend/src/main/java/com/nghd/yueju/web/AdminController.java) | 管理接口 |
| AdminService | [`AdminService.java`](../springboot-backend/src/main/java/com/nghd/yueju/service/AdminService.java) | 管理业务逻辑 |
| AdminMapper | [`AdminMapper.java`](../springboot-backend/src/main/java/com/nghd/yueju/mapper/AdminMapper.java) | 总览统计 SQL |

### 10.2 管理接口列表

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/admin/me` | GET | 获取当前管理员信息 |
| `/api/admin/overview` | GET | 平台数据总览（各表记录数） |
| `/api/admin/events?status=pending` | GET | 演出列表（按状态筛选） |
| `/api/admin/events/{id}/review` | POST | 审核演出（通过/拒绝） |
| `/api/admin/events/{id}/signups` | GET | 查看演出报名名单 |
| `/api/admin/tables` | GET | 获取可管理的数据表列表 |
| `/api/admin/table/{name}` | GET | 通用数据表浏览（搜索/过滤/分页） |

### 10.3 演出审核流程

**审核接口**（[`AdminController.java`](../springboot-backend/src/main/java/com/nghd/yueju/web/AdminController.java) 第 58-65 行）：

```java
@PostMapping("/events/{id}/review")
public Map<String, Object> review(@PathVariable String id, @RequestBody Map<String, Object> b) {
    String action = str(b.get("action"));   // "approve" 或 "reject"
    String note = str(b.get("note"));       // 审核备注
    return admin.review(id, action, note, CurrentUser.require());
}
```

**审核业务逻辑**（[`AdminService.java`](../springboot-backend/src/main/java/com/nghd/yueju/service/AdminService.java) 第 54-71 行）：

```java
public Map<String, Object> review(String id, String action, String note, AuthUser admin) {
    ShowEvent e = eventMapper.selectById(id);
    if (e == null) throw BusinessException.notFound("演出不存在");
    if (!"approve".equals(action) && !"reject".equals(action)) {
        throw BusinessException.badRequest("无效的操作");
    }
    String status = "approve".equals(action) ? "approved" : "rejected";
    e.setStatus(status);
    e.setReviewNote(note == null ? "" : note);
    e.setReviewedAt(Ids.now());
    eventMapper.updateById(e);
    audit(admin, "review_event", id + " -> " + status);

    // 清除演出列表缓存
    redisTemplate.delete("cache:shows:approved");

    return Map.of("ok", true, "event", eventService.mapEvent(eventMapper.selectById(id)));
}
```

### 10.4 演出状态流转

```
用户提交申请
    ↓
status = "pending"（待审核）
    ↓
管理员审核
    ↓
approve → status = "approved"（已通过，用户端可见）
reject  → status = "rejected"（已拒绝）
```

### 10.5 通用数据表浏览

**表定义**（[`AdminService.java`](../springboot-backend/src/main/java/com/nghd/yueju/service/AdminService.java) 第 78-127 行）：

```java
private static final Map<String, TableDef> TABLES = new LinkedHashMap<>();
static {
    TABLES.put("opera", new TableDef("剧目", "内容资料",
            List.of("title", "alias", "playwright"),   // 可搜索字段
            List.of("genre", "troupe"),                 // 可过滤字段
            cols("id:编号", "title:剧目名称", ...)));   // 列定义
    TABLES.put("artist", new TableDef("名家", "内容资料", ...));
    TABLES.put("media", new TableDef("视听资源", "内容资料", ...));
    TABLES.put("user", new TableDef("用户", "用户互动", ...));
    // ... 共 13 张表
}
```

**查询实现**（第 153-195 行）：

```java
public Map<String, Object> table(String name, String q, Map<String, String> query) {
    TableDef def = TABLES.get(name);
    if (def == null) throw BusinessException.notFound("未知数据表");

    // 构建 WHERE 条件
    List<String> conds = new ArrayList<>();
    List<Object> args = new ArrayList<>();
    if (q != null && !q.isBlank() && !def.search().isEmpty()) {
        // 关键词搜索：OR 多个字段
        List<String> ors = new ArrayList<>();
        for (String c : def.search()) { ors.add("`" + c + "` LIKE ?"); args.add("%" + q + "%"); }
        conds.add("(" + String.join(" OR ", ors) + ")");
    }
    // 过滤条件
    for (String fk : def.filters()) {
        String v = query.get(fk);
        if (v != null && !v.isBlank() && !"all".equals(v)) {
            conds.add("`" + fk + "` = ?"); args.add(v);
        }
    }

    // 分页查询
    String where = conds.isEmpty() ? "" : " WHERE " + String.join(" AND ", conds);
    Long total = jdbc.queryForObject("SELECT COUNT(*) FROM `" + name + "`" + where, Long.class, args.toArray());
    List<Map<String, Object>> rows = jdbc.queryForList(
            "SELECT " + colSql + " FROM `" + name + "`" + where + " LIMIT ? OFFSET ?", rowArgs.toArray());

    // 返回：数据行 + 列定义 + 过滤器选项
    return Map.of("name", name, "label", def.label(), "total", total,
            "rows", rows, "columns", columns, "filters", filters);
}
```

### 10.6 平台总览 KPI

**SQL**（[`AdminMapper.java`](../springboot-backend/src/main/java/com/nghd/yueju/mapper/AdminMapper.java)）：

```java
@Select("""
    SELECT
      (SELECT COUNT(*) FROM opera)        AS operas,
      (SELECT COUNT(*) FROM artist)       AS artists,
      (SELECT COUNT(*) FROM media)        AS media,
      (SELECT COUNT(*) FROM lyrics)       AS lyrics,
      (SELECT COUNT(*) FROM user)         AS users,
      (SELECT COUNT(*) FROM comment)      AS comments,
      (SELECT COUNT(*) FROM favorite)     AS favorites,
      (SELECT COUNT(*) FROM performance)  AS samples,
      (SELECT COUNT(*) FROM show_event)   AS events,
      (SELECT COUNT(*) FROM show_event WHERE status='pending')  AS pending,
      (SELECT COUNT(*) FROM show_event WHERE status='approved') AS approved,
      (SELECT COUNT(*) FROM show_event WHERE status='rejected') AS rejected,
      (SELECT COUNT(*) FROM show_signup)  AS signups
    """)
Map<String, Object> overview();
```

---

## 11. Redis 业务缓存

### 11.1 Redis 配置

**文件**：[`RedisConfig.java`](../springboot-backend/src/main/java/com/nghd/yueju/config/RedisConfig.java)

```java
@Configuration
public class RedisConfig {
    @Bean
    public RedisTemplate<String, Object> redisTemplate(RedisConnectionFactory factory) {
        RedisTemplate<String, Object> template = new RedisTemplate<>();
        template.setConnectionFactory(factory);
        // Key 使用 StringRedisSerializer
        StringRedisSerializer keySer = new StringRedisSerializer();
        // Value 使用 JSON 序列化
        GenericJackson2JsonRedisSerializer valSer =
                new GenericJackson2JsonRedisSerializer(new ObjectMapper());
        template.setKeySerializer(keySer);
        template.setHashKeySerializer(keySer);
        template.setValueSerializer(valSer);
        template.setHashValueSerializer(valSer);
        return template;
    }
}
```

**连接配置**（[`application.yml`](../springboot-backend/src/main/resources/application.yml) 第 13-17 行）：

```yaml
spring:
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      database: 1
```

### 11.2 缓存列表

| 缓存 Key | 对应接口 | TTL | 清除时机 |
|---------|---------|-----|---------|
| `cache:shows:approved` | `GET /api/shows` | 30 分钟 | 审核演出、提交新申请 |
| `cache:operas:hot` | `GET /api/operas?sort=popularity` | 30 分钟 | — |
| `cache:artists` | `GET /api/artists` | 30 分钟 | — |
| `stats:*` | `GET /api/stats/*` | 10 分钟 | — |
| `login:fail:{username}` | 登录限流计数 | 10 分钟 | 登录成功自动清除 |
| `jwt:blacklist:{token}` | JWT 注销黑名单 | JWT 剩余有效期 | — |

### 11.3 演出列表缓存

**文件**：[`EventService.java`](../springboot-backend/src/main/java/com/nghd/yueju/service/EventService.java) 第 52-68 行

```java
public List<Map<String, Object>> listShows(String status) {
    String st = status == null ? "approved" : status;
    String cacheKey = "cache:shows:" + st;

    // 1. 尝试从缓存读取
    List<Map<String, Object>> cached = (List<Map<String, Object>>) redisTemplate.opsForValue().get(cacheKey);
    if (cached != null) {
        return cached;
    }

    // 2. 缓存未命中，查询数据库
    List<Map<String, Object>> result = eventMapper.selectList(new QueryWrapper<ShowEvent>()
            .eq("status", st).orderByAsc("date").orderByAsc("time"))
            .stream().map(this::mapEvent).toList();

    // 3. 写入缓存
    redisTemplate.opsForValue().set(cacheKey, result, 30, TimeUnit.MINUTES);
    return result;
}
```

### 11.4 热门剧目缓存

**文件**：[`ContentService.java`](../springboot-backend/src/main/java/com/nghd/yueju/service/ContentService.java) 第 58-112 行

```java
public List<Map<String, Object>> listOperas(..., String sort, ...) {
    // 仅当 sort=popularity 且无其他筛选条件时缓存
    boolean isHot = ("hot".equals(sort) || "popularity".equals(sort))
            && genre == null && era == null && troupe == null
            && role == null && (kw == null || kw.isBlank()) && learn == null;

    if (isHot) {
        List<Map<String, Object>> cached = (List<Map<String, Object>>)
                redisTemplate.opsForValue().get("cache:operas:hot");
        if (cached != null) return cached;
    }

    // ... 查询逻辑 ...

    // 缓存热门剧目
    if (isHot) {
        redisTemplate.opsForValue().set("cache:operas:hot", rows, 30, TimeUnit.MINUTES);
    }
    return rows;
}
```

### 11.5 演员列表缓存

**文件**：[`ContentService.java`](../springboot-backend/src/main/java/com/nghd/yueju/service/ContentService.java) 第 145-158 行

```java
public List<Map<String, Object>> listArtists() {
    String cacheKey = "cache:artists";
    List<Map<String, Object>> cached = (List<Map<String, Object>>)
            redisTemplate.opsForValue().get(cacheKey);
    if (cached != null) return cached;

    List<Map<String, Object>> result = mapper.listArtists().stream()
            .map(this::mapArtist).collect(Collectors.toList());

    redisTemplate.opsForValue().set(cacheKey, result, 30, TimeUnit.MINUTES);
    return result;
}
```

### 11.6 缓存清除策略

**审核演出时清除**（[`AdminService.java`](../springboot-backend/src/main/java/com/nghd/yueju/service/AdminService.java) 第 69 行）：

```java
redisTemplate.delete("cache:shows:approved");
```

**提交新申请时清除**（[`EventService.java`](../springboot-backend/src/main/java/com/nghd/yueju/service/EventService.java) 第 127-128 行）：

```java
redisTemplate.delete("cache:shows:approved");
```

---

## 附录：关键技术决策说明

### A. 为什么选择 JWT 而非 Session？

- **无状态**：后端不维护 Session 状态，便于水平扩展
- **跨域友好**：Token 存储在客户端，天然支持 CORS
- **移动端适配**：App/小程序同样使用 Token 认证

### B. 为什么热度分析使用 Redis 缓存？

- `performance` 表有数万条记录，聚合查询涉及 `SUM`、`GROUP BY`、年份权重计算
- 前端热度分析页面会同时请求多个聚合接口
- 缓存后重复查询时延从秒级降至毫秒级，显著提升并发吞吐

### C. 为什么登录限流使用 Redis？

- 限流状态需要跨请求共享，内存变量无法实现
- Redis `INCR` + `EXPIRE` 是经典的滑动窗口限流方案
- 支持分布式部署场景

### D. 为什么使用 MyBatis-Plus 而非 JPA？

- 项目以读查询为主，MyBatis 注解方式更灵活
- 复杂聚合 SQL（如热度分析）用 MyBatis 更直观
- MyBatis-Plus 提供基础 CRUD，减少样板代码

### E. 为什么通用数据表浏览使用 JdbcTemplate？

- 表名动态传入，无法用 MyBatis 注解映射
- 搜索/过滤条件动态拼接，JdbcTemplate 更适合
- 返回原始 `Map<String, Object>`，前端动态渲染表格
